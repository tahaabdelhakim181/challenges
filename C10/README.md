# C10

Simple merge practice — merging feature/footer into main.

## What was done
Created branch feature/footer, added footer content, then merged into main.

## Commands used
- git checkout -b feature/footer
- git add .
- git commit -m "feat: add footer"
- git checkout main
- git merge feature/footer
